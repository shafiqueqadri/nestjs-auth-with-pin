export enum EUserRequest {
  ACCEPT = 'ACCEPTED',
  REJECT = 'REJECTED',
  NOT_RESPONDING = 'NOT_RESPONDING',
}
export enum ECodeRequest {
  REGISTRATION = 'REGISTRATION',
  LOGIN = 'LOGIN',
  UPDATE_PROFILE = 'UPDATE_PROFILE',
}

export enum ECodeRequestStatus {
  EXPIRED = 'EXPIRED',
  NOTEXPIRED = 'NOTEXPIRED',
}
